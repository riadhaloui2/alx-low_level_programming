#include "main.h"

/**
 * _abs - print the absolute
 * @n: The character to print
 * Return: On success 1.
 */

int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
		return (n);
	return (0);


}

