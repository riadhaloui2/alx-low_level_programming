#include "main.h"

/**
 * _isalpha - check if the char is an alphabet
 * @c: The character to print
 *
 * Return: On success 1.
 */

int _isalpha(int c)
{

	if ((c >= 'a' && c <= 'z') || (c >= 'A' && c <= 'z'))
		return (1);
	else
		return (0);






}
